function enviarFormulario() {
    // Aqui você pode adicionar código para enviar o formulário

    // Exibir SweetAlert de envio com sucesso
    swal("Envio com sucesso!", "Seu formulário foi enviado com sucesso!", "success");
}
